name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

env:
  PYDEPS: --with pytest==9.1.1 --with hypothesis==6.168.1 --with numpy==2.5.3 --with scipy==1.18.1 --with pyloudnorm==0.2.0 --with pyyaml==6.0.3

jobs:
  invariants:
    name: Invariants (published numbers, family curve, timeline helpers, score)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.12
      - uses: astral-sh/setup-uv@v10.2.0
      - run: bun install --frozen-lockfile
      - name: Timeline helpers (Vitest + fast-check)
        run: bunx vitest run
      - name: Rebuilding video.json from the PolicyEngine outputs changes nothing
        run: |
          uv run --with pyyaml==6.0.3 tools/adapt_outputs.py > /dev/null
          uv run --with pyyaml==6.0.3 tools/build_video_data.py
          test -z "$(git status --porcelain -- data/)" || { git status; git diff; exit 1; }
      - name: Published numbers, family curve, score (pytest + Hypothesis)
        run: uv run $PYDEPS pytest -m "not slow"

  determinism:
    name: Frames depend only on t (both layouts)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.12
      - run: bun install --frozen-lockfile
      - run: bunx playwright install --with-deps chromium
      - run: bun run build
      - run: bun run determinism
